syntax = "proto3";

package host;

// Host is a unqiue server within a Gaffer cluster.
message Host {
  // Server hostname
  string name = 1;
  // Server IP address
  string address = 2;
  // MAC Address
  string mac = 3;
  // RPC port
  int32 port = 4;
}
