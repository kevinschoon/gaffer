syntax = "proto3";

package host;

// Host is a unqiue server within a Gaffer cluster.
message Host {
  // Server hostname
  string name = 1;
  // Server IP address
  string address = 2;
  int32 port = 3;
}
